name: Java CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Cache Maven packages
        uses: actions/cache@v3
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Build with Maven
        run: mvn -B package --file pom.xml

      - name: Run Tests
        run: mvn test

  deploy:
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Heroku Authentication
        run: |
          echo "machine api.heroku.com" > ~/.netrc
          echo "  login ${{ skillfactory-calculator.HEROKU_EMAIL }}" >> ~/.netrc
          echo "  password ${{ skillfactory-calculator.HEROKU_API_KEY }}" >> ~/.netrc
          echo "machine git.heroku.com" >> ~/.netrc
          echo "  login ${{ skillfactory-calculator.HEROKU_EMAIL }}" >> ~/.netrc
          echo "  password ${{ skillfactory-calculator.HEROKU_API_KEY }}" >> ~/.netrc
        env:
          HEROKU_API_KEY: ${{ skillfactory-calculator.HEROKU_API_KEY }}
          HEROKU_EMAIL: ${{ skillfactory-calculator.HEROKU_EMAIL }}

      - name: Deploy to Heroku
        run: |
          echo "Deploying to Heroku..."
          git remote add heroku https://git.heroku.com/agile-retreat-95765.git
          git push heroku main:main